import { createSlice } from "@reduxjs/toolkit";

const user = createSlice({
    name : 'infoValidation',
    initialState : {id : false , password : false , confirmPassword : false , name : false , },
    reducers :{

    }
})