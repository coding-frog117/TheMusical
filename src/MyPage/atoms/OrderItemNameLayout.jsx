import React from 'react';
import styled from 'styled-components';

export const OrderItemNameLayout = styled.div`
	display: flex;
	flex-direction: column;
	cursor: pointer;
	gap: 10px;
	margin-left: 30px;
`;
