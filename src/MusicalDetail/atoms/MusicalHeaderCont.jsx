import React from 'react';
import styled from 'styled-components';

export const MusicalHeaderCont = styled.section`
	border-bottom: 1px solid ${(props) => props.theme.lightGray};
	padding: 25px;
`;

export default MusicalHeaderCont;
